<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Display CRUD</title>
    <style>
        table,
        th,
        td {
            border: 1px solid black;
            border-collapse: collapse;
            text-align: center;
        }

        .edit-btn {
            background-color: rgb(50, 255, 112);
            border: 0px;
            padding: 4px 8px;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            color: black;
        }

        .delete-btn {
            background-color: rgb(255, 127, 110);
            border: 0px;
            padding: 4px 8px;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            color: black;
        }

        .edit-btn:hover, .delete-btn:hover{
            opacity: 0.8;
        }

    </style>
</head>
<body>
    <table class="table table-hover" style="width:100%">
        <tr>
            <th>Email</th>
            <th>First name</th>
            <th>Last name</th>
            <th>Address</th>
            <th>Phone number</th>
            <th colspan="2">Actions</th>
        </tr>
        <tr>
            <% for(let i=0; i<dataTable.length; i++) {%>
                <tr>
                    <td>
                        <%= dataTable[i].email %>
                    </td>
                    <td>
                        <%= dataTable[i].firstName %>
                    </td>
                    <td>
                        <%= dataTable[i].lastName %>
                    </td>
                    <td>
                        <%= dataTable[i].address %>
                    </td>
                    <td>
                        <%= dataTable[i].phoneNumber %>
                    </td>
                    <td>
                        <a href="/edit-crud?id=<%= dataTable[i].id %>" class="edit-btn" type="button">Edit</a>
                    </td>
                    <td>
                        <a href="/delete-crud?id=<%= dataTable[i].id %>" class="delete-btn" type="button">Delete</a>
                    </td>
                </tr>
            <% } %>
        </tr>
    </table>
</body>
</html>